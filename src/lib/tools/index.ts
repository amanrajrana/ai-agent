export * from "./availableTools";
import { getCourses } from "./course";

export const test = async () => {
  //   await getCourses()
  //     .then((courses) => {
  //       console.dir(courses, { depth: null, colors: true });
  //     })
  //     .catch((error) => {
  //       console.error("Error fetching courses:", error);
  //     });
};

